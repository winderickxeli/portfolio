// CSS
import styles from '../CSS/styles.module.css';

const Base = () => {
    return (
        <main className={styles.Content}>
            <h1>Thanks for dropping by!</h1>
        </main>
    )
}

export default Base;